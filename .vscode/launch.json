{
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Event Management System",
            "program": "${workspaceFolder}/event_management_system/${input:entryFile}",
            "args": [
                "--serve"
            ]
        }
    ],
    "inputs": [
        {
            "type": "promptString",
            "id": "entryFile",
            "description": "Path to the entry file of your application"
        }
    ]
}